# backupurvm client agent — edit then:
#   sudo systemctl enable --now backupurvm-client
#
# Must match the host shared_key (write the secret into the key file).

BACKUPURVM_HOST=127.0.0.1:9090
BACKUPURVM_KEY=/etc/backupurvm/backup.key
BACKUPURVM_SOURCE=/root
BACKUPURVM_NAME=
